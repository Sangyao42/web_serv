#include "Parser.hpp"

namespace http_parser
{
    /* Output    ParseHttpMessage(Input input)
    {
        Output result = ParseStartLine(input);
        if (!result.is_success())
            return result;
        result = ParseCarriageReturn(result.rest);
        if (!result.is_success())
            return result;
    } */
} // namespace http_parser

